export enum NumericMode {
    VALUE = 'VALUE',
    NOTE = 'NOTE',
}

export enum BoardMode {
    COMPOSE = 'COMPOSE',
    PLAY = 'PLAY',
}

export enum Screen {
    BOARD = 'BOARD',
    SAVED_BOARDS = 'SAVED_BOARDS',
}
